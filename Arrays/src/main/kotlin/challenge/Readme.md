# Challenge
Create a function that takes an array of numbers and returns 
the biggest number in the array.

Create a second function that takes an array of numbers and returns 
the smallest number in the array.

Create a third function that takes an array of numbers and a boolean (call it useMax) and returns the
biggest (if useMax is true) or the smallest (if useMax is false) number in the array.
